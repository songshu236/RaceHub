fix: 修复表格 1x1 不可见（空窗口的真正根因）

问题：make_tree 把 Treeview 包装在带滚动条的 Frame 中返回，
但各面板只保留了 Treeview（self.X, _ = make_tree(...)），
随后把 Treeview grid 进它自己的父 Frame（该 Frame 本身从未被布局），
导致所有表格区域 1x1、未映射，窗口看起来“空”。
数据其实一直都在（日志 rows=23/22/22 等）。

修复：保存并正确布局包装 Frame（self.X_frame.grid(...)），
三个面板共 9 处；实测全部表格 mapped=1、尺寸正常。