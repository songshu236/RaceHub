fix: BIG 黑底队标真正生效（夜间版白色图标 + 透明背景转换）

- 原因：BIG 旧图标是白色 SVG 转换失败的白框；且其日间版图标为黑色，
  黑底上不可见
- 修复：MIBR/BIG 抓取 HLTV night-only（夜间/白色）图标；
  SVG->PNG 转换改透明背景（alpha=True），不再被白色页面吞掉
- 重新下载 BIG/MIBR 为白色图标（黑底显示），示例数据同步更新
