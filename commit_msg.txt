fix: 队标显示 + 字体像素对齐（DPI）

队标不显示的根因：add_badge_column（启用 #0 图片列）在
grid 布局修复后因字符串不匹配被静默跳过，三个面板都未生效。
- 补齐 6 处 add_badge_column 调用（F1 赛果/积分、WEC 赛果/积分、CS2 对阵/排名）
- 队标改为 RGB 无透明通道（圆角用面板底色），避免 Tk 透明渲染问题
- 队标尺寸 28->30，列宽同步

字体像素对齐：
- 恢复 DPI 感知（SYSTEM_DPI_AWARE 优先），Tk 按系统物理像素渲染，
  文字与系统界面像素对齐、不发虚；字号配置保持不变
- 补全之前漏掉的列宽加宽