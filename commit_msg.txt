feat: CS2 使用 HLTV 真实队标（F1/WEC 保持彩色徽章）

- cs2.py：排名页/结果页提取真实队标 URL（优先 night-only、srcset 2x）
- 新增 team_logos.py：后台下载 + data/logos 磁盘缓存 + PIL 缩放，
  并发限流(3) + CDN 失败冷却(10分钟)防请求风暴；失败自动回退彩色徽章
- cs2 面板：对阵表/排名队标列优先真实队标，下载完成后自动刷新
- fetcher.fetch_bytes 支持 cloudscraper
- 示例数据补充 215 个排名队标 + 80 个比赛队标 URL

说明：HLTV 图片 CDN 有 Cloudflare 防护且按 IP 间歇性封锁；
能访问时显示真实队标，不能访问时自动显示彩色徽章并冷却重试。