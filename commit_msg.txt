fix: 队标本地化（免重复爬）+ 白底显示

- 队标持久化：data/logos 不再 gitignore，40 个已下载真实队标入库；
  已存在的队标从磁盘加载，不再触发下载
- 新增 data/logos/_status.json 记录下载失败时间，单队 24 小时内不重试，
  避免每次启动都重新爬；连续失败进入整批冷却
- 下载队列 worker 尊重冷却，冷却期间跳过已排队项
- 队标显示改为白底（合成到白色背景+细边框），不再使用透明底